<form method="post" action="{{ path('materiel_sortie_delete', {'id': materiel_sortie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel_sortie.id) }}">
    <button class="btn">Delete</button>
</form>
